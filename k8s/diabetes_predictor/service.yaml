apiVersion: v1
kind: Service
metadata:
  name: diabetes-predictor-service
  namespace: medical
spec:
  selector:
    app: diabetes-predictor
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
