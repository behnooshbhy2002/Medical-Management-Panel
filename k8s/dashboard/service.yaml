apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: medical
spec:
  selector:
    app: dashboard
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30001
  type: NodePort
